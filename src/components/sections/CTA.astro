---
import { Container, Button } from '@/components/common';
import { ArrowRight, MessageCircle } from 'lucide-react';
---

<section class="section-padding bg-gradient-to-r from-gray-900 via-gray-800 to-gray-900 text-white relative overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute top-0 right-0 w-96 h-96 bg-primary rounded-full filter blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-primary-light rounded-full filter blur-3xl"></div>
  </div>

  <Container class="relative z-10">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Main Heading -->
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6">
        プロジェクトを始めましょう
      </h2>
      
      <!-- Description -->
      <p class="text-xl md:text-2xl text-gray-300 mb-12 leading-relaxed">
        あなたのAI・ロボティクスプロジェクトを<br class="hidden md:block">
        次のレベルへ
      </p>
      
      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16">
        <Button size="lg" class="bg-primary hover:bg-primary-dark text-white shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all" asChild>
          <a href="/request-data" class="flex items-center gap-2">
            <span>データを依頼する</span>
            <ArrowRight class="w-5 h-5" />
          </a>
        </Button>
        
        <Button variant="outline" size="lg" class="border-gray-300 text-white hover:bg-white hover:text-gray-900 transform hover:-translate-y-1 transition-all" asChild>
          <a href="#contact" class="flex items-center gap-2">
            <MessageCircle class="w-5 h-5" />
            <span>お問い合わせ</span>
          </a>
        </Button>
      </div>
      
      <!-- Contact Information -->
      <div class="border-t border-gray-700 pt-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
          <div>
            <h3 class="text-lg font-semibold mb-2 text-primary">メール</h3>
            <p class="text-gray-300">info@deephand.ai</p>
          </div>
          <div>
            <h3 class="text-lg font-semibold mb-2 text-primary">営業時間</h3>
            <p class="text-gray-300">平日 9:00-18:00</p>
          </div>
          <div>
            <h3 class="text-lg font-semibold mb-2 text-primary">対応時間</h3>
            <p class="text-gray-300">平均24時間以内</p>
          </div>
        </div>
      </div>
      
      <!-- Final Message -->
      <div class="mt-12 p-6 bg-white/5 rounded-2xl backdrop-blur-sm border border-white/10">
        <p class="text-gray-300 leading-relaxed">
          🚀 <strong class="text-white">無料相談実施中</strong> - プロジェクトの詳細をお聞かせください。<br>
          最適なアノテーション戦略をご提案いたします。
        </p>
      </div>
    </div>
  </Container>
</section>