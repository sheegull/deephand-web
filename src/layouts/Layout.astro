---
import { getAnalyticsProps } from '@/lib/analytics';

export interface Props {
  title: string;
  description?: string;
}

const { title, description = 'DeepHand - データアノテーションサービス' } = Astro.props;
const analytics = getAnalyticsProps();
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- Alliance Font Preload - AllianceNo2 TTF -->
    <link rel="preload" href="/fonts/AllianceNo2-Regular.ttf" as="font" type="font/ttf" crossorigin />
    <link rel="preload" href="/fonts/AllianceNo2-Medium.ttf" as="font" type="font/ttf" crossorigin />
    <link rel="preload" href="/fonts/AllianceNo2-SemiBold.ttf" as="font" type="font/ttf" crossorigin />
    
    <title>{title}</title>
    
    <!-- Cloudflare Web Analytics -->
    {analytics.isEnabled && (
      <Fragment set:html={analytics.script} />
    )}
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  html {
    font-family: 'Alliance', system-ui, -apple-system, sans-serif;
  }
  body {
    font-family: 'Alliance', system-ui, -apple-system, sans-serif;
  }
  .font-alliance {
    font-family: 'Alliance', system-ui, -apple-system, sans-serif;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      'Lucida Console',
      Liberation Mono,
      'DejaVu Sans Mono',
      'Bitstream Vera Sans Mono',
      'Courier New',
      monospace;
  }
</style>