---
// 🚀 Step 1: ルートページを英語に変更
import BaseLayoutEn from '@/layouts/BaseLayoutEn.astro';
import { HeroSection } from '@/components/HeroSection';
import { setCurrentLanguage } from '@/lib/i18n';
import '@/styles/app.css';

// 🚀 サーバーサイドで英語に設定
setCurrentLanguage('en');
---

<BaseLayoutEn
  title="DeepHand - AI & Robotics Data Annotation Service"
  description="DeepHand is a specialized annotation team for robotics. We provide high-quality annotation data for your AI and robotics projects."
>
  <HeroSection
    client:load
    onRequestClick={() => window.location.href = '/request'}
    onNavClick={(element) => {
      if (element === 'get-started') {
        window.location.href = '/request';
      } else if (element === 'login') {
        // ログイン機能は未実装
        console.log('Login clicked');
      } else {
        // その他のナビゲーション
        const elementId = element.replace(/\s+/g, '-');
        const targetElement = document.getElementById(elementId);
        if (targetElement) {
          targetElement.scrollIntoView({ behavior: 'smooth' });
        }
      }
    }}
    onLogoClick={() => window.location.href = '/'}
  />
</BaseLayout>
